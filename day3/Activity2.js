// Activity 2: Nested If-Else Statements

// Task 3: White a program to find the largest of three numbers using nested if-else statements

let a = 12 ;
let b= 34;
let c= 45;


if (a > b) {  
    if (a > c) {  
        console.log(`The largest number is ${a}`);
        
    } else {  
        console.log(`The largest number is ${c}`);
    }
} else {  
    if (b > c) {  
        console.log(`The largest number is ${b}`);
    } else {  
        console.log(`The largest number is ${c}`);
    }
}






